import React from 'react';

const Header = () => {
  return (
    <header className="border-b border-transparent">
      <div className="container py-4 flex justify-between items-center">
        {/* Cabeçalho simplificado sem links e botões */}
      </div>
    </header>
  );
};

export default Header;
